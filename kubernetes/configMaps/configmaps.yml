apiVersion: v1
kind: ConfigMap
metadata:
  name: my-config
data:
  # Property-style key-value pairs
  app_name: "My Application"
  app_version: "1.0.0"

  # Multi-line data (e.g., JSON or scripts)
  config.json: |
    {
      "debug": true,
      "port": 8080
    }
