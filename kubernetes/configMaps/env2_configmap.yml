apiVersion: v1
kind: ConfigMap
metadata:
  name: env-config
data:
  app.env: |
    APP_NAME=MyApp
    APP_ENV=production
    APP_DEBUG=false
    DATABASE_URL=mysql://user:pass@localhost:3306/mydb
    API_KEY=your-api-key
